{
  "name": "replyfirst-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Public demo: instant AI reply for Malaysian SMEs. Prospecting tool and demand test.",
  "scripts": {
    "dev": "vercel dev --listen 3000",
    "deploy": "vercel --prod"
  },
  "engines": {
    "node": ">=18"
  }
}
